<%= form_for([@venue, @event], url: venue_events_path(@venue), :html => { :multipart => true }) do |f| %>
 <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>
      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-md-4">

    <div class="field">
      <%= f.label :name, class: "form-label" %><br />
      <%= f.text_field :name, autofocus: true, class: "form-input" %>
    </div>

    <div class="field">
      <%= f.label 'Start Date', class: "form-label" %><br />
      <%= f.text_field :start_date, class: "form-input" %>
    </div>

    <div class="field">
      <%= f.label 'End Date', class: "form-label" %><br />
      <%= f.text_field :end_date, class: "form-input" %>
    </div>

    <div class="field">
      <%= f.label 'Start Time', class: "form-label" %><br />
      <%= f.text_field :start_time, class: "form-input" %>
    </div>

    <div class="field">
      <%= f.label 'End Time', class: "form-label" %><br />
      <%= f.text_field :end_time, class: "form-input" %>
    </div>

  </div>

  <div class="col-md-4">

    <div class="field">
      <%= f.label :address, class: "form-label" %><br />
      <%= f.text_field :address, class: "form-input" %>
    </div>

    <div class="field">
      <%= f.label :city, class: "form-label" %><br />
      <%= f.text_field :city, class: "form-input" %>
    </div>

    <div class="field">
      <%= f.label :state, class: "form-label" %><br />
      <%= f.text_field :state, class: "form-input" %>
    </div>

    <div class="field">
      <%= f.label :zip, class: "form-label" %><br />
      <%= f.text_field :zip, class: "form-input" %>
    </div>

    <div class="field">
      <%= f.label 'Facebook Event URL', class: "form-label" %><br />
      <%= f.text_field :facebook, class: "form-input" %>
    </div>

  </div>

  <div class="col-md-4">

     <div class="field">
      <%= f.label :description, class: "form-label" %><br />
      <%= f.text_area :description, class: "form-input" %>
    </div>

    <div class="field">
      <%= f.label 'Contact Name', class: "form-label" %><br />
      <%= f.text_field :contact, class: "form-input" %>
    </div>

    <div class="actions">
      <%= f.submit "Submit", class: "btn btn-success btn-lg hpbutton form-submit" %>
    </div><br>

    <%= link_to 'Back', @venue, class:'btn btn-success btn-lg hpbutton' %>

<% end%>
